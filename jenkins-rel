pipeline {
    agent { label 'shop'}
    stages {
        stage ('vcs') {
            steps {
                git url: https://github.com/gautamshrinivas/buddy-works.git,
                branch: 'rel'
            }
        }
        stage ('build') {
            steps {
                sh 'mvn package'
            }
        }
    }
}